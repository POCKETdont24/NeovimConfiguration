That piece of shit for lazy dumbass who don`t know how to make minimalistic and simple Neovim setup for C++

If you lazy enough, run install.sh (on windows you need to open Git Bash inside cloned directory), it will check installed neovim, install all needed stuff like vim-plug, and copy init.lua where is need (NOT TESTED LOL)

NEED FOR AUTOCOMPLETE:

  CLANG
  
  For windows user , you can wite inside PowerShell "winget install -e --id LLVM.LLVM", or install it manually on your own (NOT TESTED LOL)
  
  For linux or macOS user , you can download it with your favorite package manager like "sudo pacman -S clang" or some other shi

USAGE:

  ctrl + n - Open NERDTree

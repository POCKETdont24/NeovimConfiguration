That piece of shit for lazy dumbass who don`t know how to make minimalistic and simple Neovim setup for C++

If you lazy enough, run install.sh (on windows you need to open Git Bash inside cloned directory), it will check is installed neovim or not, install all needed stuff like vim-plug and copy init.lua to directory (NOT TESTED LOL)

NEED FOR AUTOCOMPLETE:

  CLANG
  
  You can download it with your favorite package manager like "sudo pacman -S clang" or some other shi

USAGE:

  ctrl + n - Open NERDTree
